# LSP configurations

This directory contains LSP-specific configurations in `lsp_name.lua` files.
LSPs must also be installed by specifying their name in the corresponding
global list in lua/plugins/lang-servers-and-tools.lua.
